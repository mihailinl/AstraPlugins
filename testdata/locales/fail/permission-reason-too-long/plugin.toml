[plugin]
id = "fixture"
name = "Fixture"
version = "0.1.0"
description = "A fixture for the shared locale rule corpus"

[entry]
command = "./bin/fixture"

[capabilities]
tools = true

[permissions]
fire_trigger = { reason = "Fires the trigger you configure whenever the plugin decides that it is the right moment to do so, which is a sentence written here purely to go over the schema's own one hundred and forty character ceiling." }
